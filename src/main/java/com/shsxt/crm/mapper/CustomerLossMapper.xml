<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shsxt.crm.dao.CustomerLossDao">
    
    <sql id="CustomerLoss_Columns">
        id, cusNo, cusName, cusManager, lastOrderTime, confirmLossTime, state, lossReason, isValid, createDate, updateDate
    </sql>

    <select id="selectForPage" resultType="CustomerLoss">
        select <include refid="CustomerLoss_Columns" /> from t_customer_loss
        <where>
            isValid = 1
            <if test="@Ognl@isNotEmpty(cusName)">
                and cusName like '%${cusName}%'
            </if>
            <if test="@Ognl@isNotEmpty(cusManager)">
                and cusManager like '%${cusManager}%'
            </if>
            <if test="@Ognl@isNotEmpty(state)">
                and state = #{state}
            </if>
        </where>
    </select>

    <insert id="insert">
        insert into  t_customer_loss (
          cusNo, cusName, cusManager, lastOrderTime, confirmLossTime, state, lossReason, isValid, createDate, updateDate
        ) values(
          #{cusNo}, #{cusName}, #{cusManager}, #{lastOrderTime}, null, 0, null, 1, now(), now()
        );
    </insert>

    <select id="loadById" resultType="CustomerLoss">
        select <include refid="CustomerLoss_Columns" /> from t_customer_loss where id = #{id}
    </select>

    <update id="update">
        update t_customer_loss
        <set>
            <if test="confirmLossTime != null">
                confirmLossTime = #{confirmLossTime},
            </if>
            <if test="@Ognl@isNotEmpty(lossReason)">
                lossReason = #{lossReason},
            </if>
            <if test="state != null ">
                state = #{state},
            </if>
            UpdateDate = now()
        </set>
        where id = #{id}
    </update>

</mapper>